# Optional. Without this key the app runs in deterministic demo mode.
OPENAI_API_KEY=

# Cost-sensitive default. Change to another Responses API model if desired.
OPENAI_MODEL=gpt-5.6-luna

PORT=3000
