{
  "name": "intent-commit",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "description": "An open protocol and deployable reference client for AI-mediated reflective human communication.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "verify": "node packages/verifier/cli.js",
    "test": "node --check server.js && node --check public/app.js && node --check packages/verifier/cli.js && node --test"
  },
  "engines": {
    "node": ">=22.13"
  }
}
